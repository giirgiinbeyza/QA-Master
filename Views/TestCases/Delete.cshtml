@model DermaLogic.Models.TestCase

@{
    ViewData["Title"] = "Testi Sil";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow border-0 border-top border-danger border-4">
                <div class="card-body p-5 text-center">
                    
                    <div class="mb-4">
                        <span style="font-size: 3rem;">🗑️</span>
                    </div>

                    <h3 class="text-danger fw-bold mb-3">Bu Test Senaryosunu Silmek İstiyor musunuz?</h3>
                    <p class="text-muted">
                        Bu işlem geri alınamaz. Silinen test verileri kalıcı olarak kaybolur.
                    </p>

                    <div class="alert alert-light border d-inline-block text-start p-3 mb-4 w-100">
                        <div class="row">
                            <div class="col-md-12 mb-2">
                                <strong class="text-primary">Başlık:</strong> @Html.DisplayFor(model => model.Title)
                            </div>
                            <div class="col-md-6">
                                <strong>Modül:</strong> @Html.DisplayFor(model => model.TestModule.Name)
                            </div>
                            <div class="col-md-6">
                                <strong>Öncelik:</strong> 
                                @if(Model.Priority == "Kritik" || Model.Priority == "Yüksek") {
                                    <span class="text-danger fw-bold">@Model.Priority</span>
                                } else {
                                    <span>@Model.Priority</span>
                                }
                            </div>
                        </div>
                    </div>

                    <form asp-action="Delete">
                        <input type="hidden" asp-for="Id" />
                        <div class="d-flex justify-content-center gap-2">
                            <a asp-action="Index" class="btn btn-secondary px-4">İptal / Vazgeç</a>
                            <input type="submit" value="🔥 Evet, Sil" class="btn btn-danger px-4" />
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>